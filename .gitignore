# Dependencies
node_modules/

# Build output
dist/
.astro/

# Environment
.env
.env.local
.env.*.local
!.env.example

# Netlify
.netlify/

# Test / coverage
coverage/

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/*
!.vscode/extensions.json

# Observation-phase telemetry — COMMITTED so the 7-day yield metrics (item
# #31) accumulate across CI runs (ephemeral runners would otherwise lose
# them). Append-only JSONL; merge races handled by the workflow's push retry.
# audit/telemetry/*.jsonl

